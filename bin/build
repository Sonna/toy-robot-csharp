#!/usr/bin/env bash

# csc ToyRobot/*.cs -out bin/Main.exe
dotnet publish ToyRobot \
  --self-contained \
  --runtime osx.10.11-x64

# dotnet publish ToyRobot --self-contained --runtime osx.10.11-x64
# dotnet publish ToyRobot --self-contained --runtime win7-x64
# dotnet publish ToyRobot --self-contained --runtime ubuntu.16.04-x64
